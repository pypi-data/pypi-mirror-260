from typing import Any


def load_obj(_: str) -> Any:
    raise Exception('this function is a stub to be replaced at runtime. This should not execute')


def save_obj(_: str, obj: Any) -> None:
    raise Exception('this function is a stub to be replaced at runtime. This should not execute')
