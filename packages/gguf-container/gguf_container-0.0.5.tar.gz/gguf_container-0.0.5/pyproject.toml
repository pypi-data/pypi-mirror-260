[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"

[project]
name = "gguf-container"
authors = [{name = "calcuis", email = "info@calcu.io"}]
description = "gguf-container (full package + local frontend)"
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version"]
dependencies = ["gguf-core", "gguf-connector", "gguf-selector", "llama-core", "callgg", "cgg"]

[project.urls]
Homepage = "https://github.com/calcuis/container-smart-contract"
Issues = "https://github.com/calcuis/container-smart-contract/issues"