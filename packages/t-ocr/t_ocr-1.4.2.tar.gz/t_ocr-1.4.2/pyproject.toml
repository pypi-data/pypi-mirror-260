[project]
name = "t_ocr"
version = "1.4.2"
authors = [
  { name="TA", email="support@thoughtful.ai" },
]
description = "OCR library"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]

build-backend = "setuptools.build_meta"
