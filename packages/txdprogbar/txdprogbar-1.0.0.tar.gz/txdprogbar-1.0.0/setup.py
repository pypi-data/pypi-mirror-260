from distutils.core import setup
import setuptools
packages = ['txdprogbar']
setup(name='txdprogbar',
    version='1.0.0',
    author='唐旭东',
    packages=packages,
    package_dir={'requests': 'requests'})