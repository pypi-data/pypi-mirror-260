[tool.poetry]
name = "pythonads"
version = "0.1.0"
description = "A sustainable, terminal-based ad network, passing 90% of revenues through to developers."
license = "MIT"
authors = ["WillDenby <119456795+WillDenby@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
