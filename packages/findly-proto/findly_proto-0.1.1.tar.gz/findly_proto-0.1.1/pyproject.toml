[tool.poetry]
name = "findly-proto"
version = "0.1.1"
license = "GPL-3.0-only"
description = ""
authors = []
readme = "README.md"
packages = [
    { include = "findly", from = "src" },
]
include = [
    "src/findly/proto/*",
]

[tool.poetry.dependencies]
python = "^3.9"
protobuf = "^4.25.3"

[tool.poetry.group.typing.dependencies]
mypy = "^1.3.0"
types-protobuf = "^4.23.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"