[build-system]
requires = [
    "setuptools>=60",
    "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ExplanationText"
version = "0.2.2"
authors = [
  { name="Tobias Litzenberger", email="tobias.litzenberger@tu-dortmund.de" },
  { name="Celine Wald", email="celine.wald@tu-dortmund.de" },
  { name="Alisa Gromova", email="alisa.gromova@tu-dortmund.de" }
]
description = ""
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
  "google-cloud-vision", "requests", "wikipedia-api", "nltk", "inflect", "transformers"
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
