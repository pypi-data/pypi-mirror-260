[project]
name = "fibonacci_package"
version = "0.3.1"
description = "A simple package for calculating Fibonacci series"
readme = "README.rst"
authors = [{name = "Saeed Foroutan", email = "foroutansaeed@live.com"}]
license = {text = "MIT License"}
dependencies = []

[project.urls]
Homepage = "https://github.com/saeedforoutan/fibonacci_package/"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["fibonacci_package"]
