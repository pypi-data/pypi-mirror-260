[tool.poetry]
name = "allokate"
version = "0.1.0"
description = ""
authors = ["Johannes Roos <jhnnsrs@gmail.com>"]
readme = "README.md"
license = "MIT"
packages = [{ include = "allokate" }]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
