[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "Cython>=3.0.8",
    "pybind11>=2.11.1",
    "ninja; sys_platform != 'Windows'",
    "cmake>=3.18",
]

build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "tests",
]
